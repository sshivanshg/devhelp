node_modules/
dist/
*.log
.env
.env.local
.DS_Store
.npmrc
CLAUDE.md
# Test suite, fixtures, and the stress-test harness are kept local and not
# tracked in the repo. The npm package already excludes them (package.json
# "files" only ships dist/), and CI relies on the build + CLI smoke tests.
test/
test-fixtures/
stress-test/
overnight-log.md
docs/trial-*.md
run-regression.sh
